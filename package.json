{
  "name": "proyectoweb",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "npm install --prefix node-api",
    "start": "npm --prefix node-api start",
    "dev": "npm --prefix node-api run dev",
    "seed": "npm --prefix node-api run seed"
  },
  "dependencies": {
    "cookie-parser": "^1.4.7",
    "dotenv": "^17.3.1"
  },
  "devDependencies": {
    "chai": "^6.2.2",
    "mocha": "^11.7.5"
  }
}
